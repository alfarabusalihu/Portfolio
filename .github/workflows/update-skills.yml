name: Auto-Update Skills from CV

on:
  schedule:
    # Run at 00:00 on the 1st day of every month
    - cron: '0 0 1 * *'
  workflow_dispatch: # Allow manual trigger button

jobs:
  update-skills:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Needed to push changes

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: npm ci || npm install # Install pdf-parse and others

      - name: Run AI Update Script
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          NEXT_PUBLIC_GOOGLE_API_KEY: ${{ secrets.NEXT_PUBLIC_GOOGLE_API_KEY }}
          DRIVE_FOLDER_ID: ${{ secrets.DRIVE_FOLDER_ID }}
          NEXT_PUBLIC_DRIVE_FOLDER_ID: ${{ secrets.NEXT_PUBLIC_DRIVE_FOLDER_ID }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: node scripts/update-cv-data.js

      - name: Commit and Push Changes
        run: |
          git config --global user.name 'Portfolio Bot'
          git config --global user.email 'bot@noreply.github.com'
          
          # Check if there are changes across all data files
          if [[ -n $(git status -s src/data/generated-skills.json src/data/cv-metadata.json src/data/projects.json) ]]; then
            git add src/data/generated-skills.json src/data/cv-metadata.json src/data/projects.json
            git commit -m "chore: auto-update skills & projects from CV [skip ci]"
            git push
            echo "Successfully pushed updates."
          else
            echo "No changes detected."
          fi
